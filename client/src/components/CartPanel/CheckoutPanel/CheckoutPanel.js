import React from "react";
import { Col, Row, Modal } from 'react-bootstrap'

// handlepayment function which enables modal to enter payment details

const CheckoutPanel = props => (
  <Row/>
);

export default CheckoutPanel;